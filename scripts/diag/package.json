{
  "name": "frontend-diagnostics",
  "version": "1.0.0",
  "description": "Frontend diagnostic and crawling scripts",
  "scripts": {
    "crawl": "npx playwright test crawl.spec.ts --reporter=html",
    "install-playwright": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}